// @ts-ignore
function printf(msg: string, value: boolean): void;


// @ts-ignore
function main(): int {

    // @ts-ignore
    let a: bigint = 1;
    let b: boolean = !true;
    let e: boolean = true;
    // @ts-ignore
    let c: double = 0.0;

    printf("Value is %d", (e && b));

    return 0;
}