// @ts-ignore
function printf(msg: string, value: boolean): void;


// @ts-ignore
function main(): int {

    let a: bigint = 1;
    let b: boolean = true;
    let e: boolean = true;
    let c: double = 0.0;

    printf("Value is %d", (e || b));

    return 0;
}