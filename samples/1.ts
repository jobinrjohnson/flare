// @ts-ignore
function printf(msg: string, value: int): void;


// @ts-ignore
function main(): int {

    let a: bigint = 1;
    let b: boolean = false;

    printf("Value is %d", (a - b));

    return 0;
}