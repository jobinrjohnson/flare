function main(): int {

    class a {
        function hello(): int {
            let x: double = 12;
            let m = x / 2;
            console.log(m);

            return 0;
        }
    }

    return 0;
}